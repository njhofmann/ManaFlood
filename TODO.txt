-Deal with timeshifted cards
-Create blank database file
-For keys with enum types in db, add checks (generic mana) that given string matches query for types table
-Update text on cards
-Comprehensive readme explaining project structure and plan

DeckInstance
-Internal list of cards in deck instances
-Map of categories to cards (reference)
-Map of card to quantity
-Cards are ids (as name), get all info only when selected

Card
-Name
-Text
-
